private Double calculate(SIGN sign, Double arg1, Double arg2) {
    switch (sign) {
        default:
            case DIVIDE:
                return arg1 / arg2;
            default
            break;
            if (operator === "DIVIDE") {
                result = arg1 / arg2;
            }
    }
    return 0d;
}